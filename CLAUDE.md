# Claude project entrypoint

@AGENTS.md

This file is the Claude Code compatibility entrypoint. The import above loads
the authoritative repository working agreement without maintaining a second
copy of its rules.

Claude-specific settings, read-only subagents, and skill adapters live in
`.claude/`. Canonical reusable skill content remains in `.agents/skills/`.
