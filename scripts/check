#!/bin/sh
set -eu

repository_root=$(CDPATH= cd -- "$(dirname -- "$0")/.." && pwd)
gate=${1:-affected}
if [ "$#" -gt 0 ]; then
  shift
fi
exec python3 "$repository_root/.harness/harness.py" --root "$repository_root" check "$gate" "$@"
